#ifndef CLAMP_LAUNCHER_H
#define CLAMP_LAUNCHER_H

#include "../clamp/includes/clamp.h"

class clamp_launcher
{
public:
    clamp_launcher();
    int start(clamp_args * args);
};

#endif // CLAMP_LAUNCHER_H
